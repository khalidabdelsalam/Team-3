apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.analytics.name }}-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  DB_PASSWORD: {{ .Values.postgres.password | default "password" | b64enc | quote }}
